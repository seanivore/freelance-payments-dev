# BUG_01_015 Checkout Session Return URL Redirect 

---

## Testing Status 

* **TEST FILE:** uid-unc-480.json 
* **BLOCKER:** State management accurately recording and accurately loading where user is placed upon returning to the site for session and payment_2 

---

## Bug History Reverse Chronological 

  1. `BUG_01_015` this is a continuation of 
  2. `BUG_01_013` - Return URL Still Redirects to Contract (JSON Shows All Null Timestamps)
  3. `BUG_01_009` - Return URL Redirects to Contract Instead of Completion Page
  4. `BUG_01_001` - Return URL 404 Redirect Failure

### Bug Details 

  + Immediately after the payment is made user is sent to the proper return_URL, gets 404 NOT FOUND 
    - But then takes the user to the Contract?? 
    - The payment is complete according to Stripe 

  + The event tracking **IS NOT RELEVANT** to the page showing immediately after making payment, see `assets/docs/v5/testing/BUG_01_016.md`

### Historic Details & Update 

  + Why did this issue start happening? 
    - We had a "payment confirmed" page. 
    - I submitted the bug because I didn't think it was the return_url page that was supposed to show 
  + This was my mistake
    - The information was for the admin and not the user, which was confusing 
    - The return_URL confirmation is redirected away from `/uid-xxx-xxx?session_id={CHECKOUT_SESSION_ID}` 
    - Sent to a clean URL `dev.payments.august.style/uid-xxx-xxx`, which was also confusing to me, **but I get it now!**
  + My request and assumption at the time was that we should handle it during the visual redesign after things were functional 
    - I said this not understanding the full extent of the way React/TS works 
    - I think I might have a solution now that I was digging and better understand how "states" control what is rendered 

### Solution Discovery 

  + **I started out asking myself:** "where do we make this "return_url" for checkout_session_1 and checkout_session_2 look the way we want it to for each instance?"

  + In /src/components/ we have both of these:
    - `src/components/Complete.tsx`
    - `src/components/CompletionView.tsx`

  + When we did that guide yesterday to set up the Checkout Session, it gave us this version of the code: 
    - GUIDE: `assets/docs/v5/QUICKSTART_CHECKOUT_SESSIONS/build_checkout_page_session_api_guide.md`
    - GUIDE GENERATED EXAMPLE CODE: `assets/docs/v5/QUICKSTART_CHECKOUT_SESSIONS/complete.jsx`

  + I realized that the language here is very specific and careful and that we were making things more limiting than need be: 

    3. Retrieve a Checkout session
       As soon as your return page loads, immediately make a request to the endpoint on your server. 
       Use the Checkout Session ID in the URL to retrieve the status of the Checkout Session.
       **`complete.jsx` Line 4-44**

    4. Handle session
       Handle the result of the session by using its status:
       `complete`: The payment succeeded. Use the information from the Checkout Session to render a success page.
       `open`: The payment failed or was canceled. Remount Checkout so that your customer can try again.
       **`complete.jsx` Line 46-80**

### Understanding **Clicks** 

  + I do now see that, on the demo `complete.jsx` it shows the *same details* 
    - That I was saying were confusing me 
    - Made me think that it wasn't showing the right page 
    - Because it "looks like it is information for the admin and not the user" 

```js complete.jsx
    return (
      <div id="payment-status">
        <div id="status-icon" style={{backgroundColor: iconColor}}>
        {icon}
      </div>
      <h2 id="status-text">{text}</h2>
      <div id="details-table">
        <table>
          <tbody>
            <tr>
              <td className="TableLabel">Payment Intent ID</td>
              <td id="intent-id" className="TableContent">{paymentIntentId}</td>
            </tr>
            <tr>
              <td className="TableLabel">Status</td>
              <td id="intent-status" className="TableContent">{status}</td>
            </tr>
            <tr>
              <td className="TableLabel">Payment Status</td>
              <td id="session-status" className="TableContent">{paymentStatus}</td>
            </tr>
            <tr>
              <td className="TableLabel">Payment Intent Status</td>
              <td id="payment-intent-status" className="TableContent">{paymentIntentStatus}</td>
            </tr>
          </tbody>
        </table>
```

  + The code shows the "payment intend ID" that confused me in the table the code is building above 💡💡💡
    - LIGHT BULB MOMENT: We're supposed to insert our custom information we want our confirmation page to say right there 
    - This is where wwe design what LINE 266-273 of [PROJECT_OVERVIEW.md](../PROJECT_OVERVIEW.md) states (I'll paste below)

### Understanding *Confirmed*

  + Based on the language in the instructions, the "status ID" tag on our `return_url` is just to help know which state to load 
    - I realize all phases — contract, invoice — all *also* show the same "clean" url `dev.payments.august.style/uid-xxx-xxx`
    - Which to me says "we can edit the URL for confirmation as we please" 

  + To me, "Use the Checkout Session ID in the URL to retrieve the status of the Checkout Session" implies 
    - The rest of the URL before it isn't what we're looking for, meaning
    - **We have room to do what we need to set it up for two different states that are payment_1 and payment_2**

  + Regardless of base URL, within reason, the code looking for **checkout session ID** still fulfills the guide's instructions that are: 
    - "As soon as your return page loads, immediately make a request to the endpoint on your server." 
    - "Use the Checkout Session ID in the URL to retrieve the status of the Checkout Session." 

  + Further, can't we use the same technique they are using with the session_id to tell our version of `complete.jsx` what state to render? 
    - If `?session_id={CHECKOUT_SESSION_ID}` is used in the URL to say what the code should do and render 
    - Then let's use, right before it, `?complete=payment_1` or `?complete=payment_2` 
    - And edit the code accordingly so that the state it loads, and thus the UI that renders, depends on which it finds 

  **Assuming I'm on track... which, it feels right, because this is how it has always worked in marketing...**

--- 

## Plan For Your Review 

### Add URL Parameter To Control State 

* **Differentiate between the two payments of each job right on it's JSON file's return_url** 

  1. `data.checkout_session_1.return_url` = https://dev.payments.august.style/uid-xxx-xxx?complete=payment_1?session_id={CHECKOUT_SESSION_ID}
  2. `data.checkout_session_2.return_url` = https://dev.payments.august.style/uid-xxx-xxx?complete=payment_2?session_id={CHECKOUT_SESSION_ID}

### Adjust What State Does What 

* **Improve the logic in the `complete.jsx` file of our own**

  1. Double check that example code to make sure we're on track: `complete.jsx`
  2. Confirm that this still behaves properly
    - As soon as your return page loads, immediately make a request to the endpoint on your server
    - Use the Checkout Session ID in the URL to retrieve the status of the Checkout Session
  3. Identify which of our files we are using, because my understanding of React is coming together, so we don't need both, right? 
    - `src/components/Complete.tsx`
    - `src/components/CompletionView.tsx`
  4. Build on, and make the if statement that is right before what to display on the page, better fits our new needs 
    - if (data.status === 'complete' and 'payment_1') 
    - Or payment_2 (idk how it is actually written hah) 

    ```jsx
            if (data.status === 'complete') {
              setIconColor('#30B130');
              setIcon(SuccessIcon);
              setText('Payment succeeded');
            } else {
              setIconColor('#DF1B41');
              setIcon(ErrorIcon);
              setText('Something went wrong, please try again.');
            }
          });
      }, []);
    ```

### Change What UI Renders Based On State 

* **STATE 1:** User completes payment_1 checkout session successfully 
  
  + This page is shown this ONE time, and then never again because next login would send them to balance PDF

  + Triggers event that 
    - Adds timestamp to JSON for `state.client_status.payment_1` in batch update (**still needs to be fixed but see other bug report**)
    - Immediately direct user to `data.checkout_session_1.return_url` 
      `https://dev.payments.august.style/uid-xxx-xxx?complete=payment_1?session_id={CHECKOUT_SESSION_ID}` 

  **STATE 1 PAGE VISUAL DESIGN AND COPY** 

  + Some kind of visual that makes them feel proud 
    - Encouraged, excited, forget they spent money 
    - A thank you, and then any next step details to be aware of 
  + Message that they will get an email for next payment 
    - Can map the job's JSON value `price2.pay_by` which is usually just "before launch" 
    - Download docs button for Contract and Invoice that *also* closes the browser (?)
  + SMALL button that says "You can pay your final balance now if you want -->" 
    - Not because we expect people to or are telling them to
    - But it feels like, given it is a payment platform, they should be able to if they happen to want for whatever reason 

* **STATE 2:** User completes payment_2 checkout session successfully 

  + This page is shown **FOREVER** 
    - Anytime the user logs in from now on 
    - It means all of `state.client_status` is filled in: `logged_in`, `contract_signed`, `invoice`, `payment_1`, `balance`, `payment_2` 
    - The user might never log in again, but if they did for whatever reason, this is the only state they should see and be able to navigate to 

  + Triggers event that 
    - Adds timestamp to JSON for `state.client_status.payment_2` in back update (**same note as above**)
    - Immediately redirects user to `data.checkout_session_2.return_url` 
      `https://dev.payments.august.style/uid-xxx-xxx?complete=payment_2?session_id={CHECKOUT_SESSION_ID}` 

  **STATE 2 PAGE VISUAL DESIGN AND COPY** 

  + Hooray visual that they've completely paid for their `product.name` (mapped JSON values)
    - Similar happy vibe, let them know that they've paid off their entire service charge of `product.service_usd` (format $0.00)
    - "Thank you `customer.name`, `customer.title` of `customer.business`" 
  + Provide for them buttons to download their documents "Don't forget to grab copies now if you didn't already" 
    - CONTRACT PDF, INVOICE PDF, BALANCE PDF 
    - And some kind of "Email sean@august.style if you have any other questions about your service" 
    - "If you'd like to submit a review, please click here" or "email review@august.style" 

  + NOTE: that when I say "page" it is because I'm new to React/TS and am not confident on my wording about states and whatnot

---

## Previous Bug Log Note 

  + In the bug logs you wrote "This happens because the state management checks `data.state.client_status.payment_1` which is still `null`"
    - Probably the original BUG_01_001 - Return URL 404 Redirect Failure
  + But note that this is only true for 
    - Return users that have all `data.state.client_status` up to `payment_2` timestamped 
    - If the user had 'null' for `data.state.client_status.payment_1` then it would route the user to the payment screen and load the checkout session 

---

## Console from Redirect After Payment (it should show state described above)

      GET https://dev.payments.august.style/uid-unc-480?session_id=cs_test_a1gxZXMC9FdCJs1a35v5bVvVvsk7Kr0l3izlDsAmerGFs3BRboc8vUTizt 404 (Not Found)
    (anonymous) @ js.stripe.com/clover/stripe.js:1
    o @ js.stripe.com/clover/stripe.js:1
    (anonymous) @ js.stripe.com/v3/fin…a9cf67e9caca22.js:1
    Promise.then
    (anonymous) @ js.stripe.com/v3/fin…a9cf67e9caca22.js:1
    (anonymous) @ js.stripe.com/v3/fin…a9cf67e9caca22.js:1
    u._fetch @ js.stripe.com/v3/fin…a9cf67e9caca22.js:1
    u.enqueue @ js.stripe.com/v3/fin…a9cf67e9caca22.js:1
    u.enqueueOne @ js.stripe.com/v3/fin…a9cf67e9caca22.js:1
    Ie @ js.stripe.com/v3/fin…a9cf67e9caca22.js:1
    (anonymous) @ js.stripe.com/v3/fin…a9cf67e9caca22.js:1
    (anonymous) @ js.stripe.com/v3/fin…a9cf67e9caca22.js:1
    l @ job-CGBR-fQ2.js:50
    pv @ index-CiALDdqw.js:8
    (anonymous) @ index-CiALDdqw.js:8
    Bi @ index-CiALDdqw.js:8
    Qc @ index-CiALDdqw.js:8
    Pc @ index-CiALDdqw.js:9
    Z1 @ index-CiALDdqw.js:9
    job-CGBR-fQ2.js:50 Vite: job.tsx loaded
    job-CGBR-fQ2.js:50 Detected session_id: cs_test_a1gxZXMC9FdCJs1a35v5bVvVvsk7Kr0l3izlDsAmerGFs3BRboc8vUTizt (from URL)
    job-CGBR-fQ2.js:1 ✅ Loaded job data for uid-unc-480: {logged_in: null, contract_signed: null, invoice: null, payment_1: null, balance: null, …}
    job-CGBR-fQ2.js:50 🔍 State Management Debug: {sessionId: 'cs_test_a1gxZXMC9FdCJs1a35v5bVvVvsk7Kr0l3izlDsAmerGFs3BRboc8vUTizt', client_status: {…}}client_status: balance: nullcontract_signed: nullinvoice: nulllogged_in: nullpayment_1: nullpayment_2: null[[Prototype]]: Objectconstructor: ƒ Object()hasOwnProperty: ƒ hasOwnProperty()isPrototypeOf: ƒ isPrototypeOf()propertyIsEnumerable: ƒ propertyIsEnumerable()toLocaleString: ƒ toLocaleString()toString: ƒ toString()valueOf: ƒ valueOf()__defineGetter__: ƒ __defineGetter__()__defineSetter__: ƒ __defineSetter__()__lookupGetter__: ƒ __lookupGetter__()__lookupSetter__: ƒ __lookupSetter__()__proto__: (...)get __proto__: ƒ __proto__()set __proto__: ƒ __proto__()sessionId: "cs_test_a1gxZXMC9FdCJs1a35v5bVvVvsk7Kr0l3izlDsAmerGFs3BRboc8vUTizt"[[Prototype]]: Object
    job-CGBR-fQ2.js:50 📍 Routing: no progress detected → contract (default)
    job-CGBR-fQ2.js:50 ✅ Final routing decision: contract
    job-CGBR-fQ2.js:50 Event: contract_loaded {page: 1, totalPages: 8}
    [...]
    job-CGBR-fQ2.js:49 Starting render for page 8...
    job-CGBR-fQ2.js:49 Page 8 render completed
    job-CGBR-fQ2.js:49 Page 8 rendered successfully
    job-CGBR-fQ2.js:50 ⚠️ Session complete but no updates needed (payment already recorded?)
# BUG_01_016 `user-exit-events.yml` issues & Return User State Placement 

---

## Testing Status 

* **TEST FILE:** uid-unc-480.json 
* **BLOCKER:** State management accurately recording and accurately loading where user is placed upon returning to the site for session and payment_2 

---

## Bug History Reverse Chronological 

  1. `BUG_01_016` this is a continuation and/or variation of 
  2. `BUG_01_014` - State Management Still Shows All Null Timestamps on Second Login
  3. `BUG_01_012` - Multiple Workflow Runs Still Occurring, Payment Events Not Recorded
  4. `BUG_01_011` - State Management Ignores All Timestamps on Second Login
  5. `BUG_01_010` - User Exit Events Workflow Runs Multiple Times, Fails to Process Payment
  6. `BUG_01_003` - User Exit Events Workflow Runs Twice, Creates Conflicts

### Bug Details 

  + User exits flow at **{{CURRENT PROBLEM POINT}}** and state is not recorded accurately 
    - This is currently happening after the User makes a payment 
    - The most recent case, there is a conflict and the main `data.state.client_status.payment_1` is never updates on JSON 

  + The payment process for payment_1 is completed according to the Stripe logs 

  + The events came through on Vercel API log `assets/docs/RESOURCES/uid-unc-480-vercel-api-logs.json` 
    - They came in separate events for that workflow 
    - Second workflow as not attempted to be committed at all 

  + Obviously it would be ideal just to be able to get all the events, best visible in that Vercel API log `assets/docs/RESOURCES/uid-unc-480-vercel-api-logs.json`, into just a single workflow, 
    - But GIT in these workflows working without flaw is an okay secondary in theory 
    - Also, what is the deal with the `dist` directory? Like this isn't because of not building things frequently enough, is it? 

  + Please use GitHub CLI to explore the logs in depth 
    - Is it possible one of the later jobs are saving it in a temp that is then being deleted? 
    - Jobs run for each workflow 
        Set up job
        Run actions/checkout@v4
        Set up Python
        Install dependencies
        Process Exit Events
        Commit Changes
        Post Set up Python
        Post Run actions/checkout@v4
        Complete job

  + Have these been inspected closely? 
    - `.github/scripts/orchestration/user_behavior.py`
    - `.github/scripts/orchestration/user_exit_events.py`

  + Or maybe this one has an idea for solution? 
    - `.gitconfig-smart-push.sh` 

  + Second log-in 1 hour later 
    - Should have sent me to make payment_1 based on the logging that **DID** go through 
    - Sent me to the contract
    - Didn't read any of the events (see the console output at bottom) 

### GitHub Action Workflow Logs 

* **FIRST WORKFLOW: User Exit Events #34**

  + COMMIT: 1d14aad — super weird glitch but this is a commit that occurred before testing started 
  + STATUS: Success 

  + Logging attempted and succeeded: 
    - `contract.signatures.client.legal_name` — ✅
    - `contract.signatures.client.signed_date` — ✅
    - `data.state.client_status.logged_in` — ✅
    - `data.state.client_status.contract_signed` — ✅
    - `data.state.client_status.invoice` — ✅

  1. PROCESS EXIT EVENTS 

```
      Run # Store payload for potential re-processing
        # Store payload for potential re-processing
        echo "$PAYLOAD_JSON" > /tmp/event_payload.json
        python .github/scripts/orchestration/user_exit_events.py || EXIT_CODE=$?
        if [ "${EXIT_CODE:-0}" -eq 2 ]; then
          echo "⚠️  Merge conflict detected - will resolve in commit step"
          exit 0  # Don't fail workflow, let commit step handle it
        elif [ "${EXIT_CODE:-0}" -ne 0 ]; then
          exit $EXIT_CODE  # Fail for other errors
        fi
        shell: /usr/bin/bash -e {0}
        env:
          pythonLocation: /opt/hostedtoolcache/Python/3.11.14/x64
          PKG_CONFIG_PATH: /opt/hostedtoolcache/Python/3.11.14/x64/lib/pkgconfig
          Python_ROOT_DIR: /opt/hostedtoolcache/Python/3.11.14/x64
          Python2_ROOT_DIR: /opt/hostedtoolcache/Python/3.11.14/x64
          Python3_ROOT_DIR: /opt/hostedtoolcache/Python/3.11.14/x64
          LD_LIBRARY_PATH: /opt/hostedtoolcache/Python/3.11.14/x64/lib
          STRIPE_SECRET_KEY: ***
          JOB_ID: uid-unc-480
          PAYLOAD_JSON: [{"type":"logged_in","timestamp":"2026-01-17T15:38:26.386Z","data":{}},{"type":"contract_loaded","timestamp":"2026-01-17T15:38:27.526Z","data":{"page":1,"totalPages":8}},{"type":"contract_signed","timestamp":"2026-01-17T15:38:44.014Z","data":{"signed_date":"2026-01-17","legal_name":"David Drogon"}},{"type":"contract_loaded","timestamp":"2026-01-17T15:38:44.477Z","data":{"page":1,"totalPages":1}},{"type":"invoice_docs","timestamp":"2026-01-17T15:38:48.168Z","data":{"choice":"no"}},{"type":"invoice","timestamp":"2026-01-17T15:38:50.003Z","data":{}}]
      Processing 6 events for uid-unc-480...
        ✓ Updated logged_in: 2026-01-17T15:38:26.386Z
        ✓ Updated contract_signed: 2026-01-17T15:38:44.014Z
        ✓ Updated contract signature legal_name: David Drogon
        ✓ Updated contract signature signed_date: 2026-01-17
        ✓ Updated invoice: 2026-01-17T15:38:50.003Z
      ✅ Successfully updated assets/jobs/uid-unc-480.json
```

  2. COMMIT CHANGES 

```
      Run git config --global user.name 'github-actions[bot]'
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        
        # CRITICAL FIX for BUG_01_010: Improved git operations to prevent conflicts
        # Strategy: Fetch latest, merge/rebase cleanly, handle conflicts by re-processing
        
        # Fetch latest changes
        git fetch origin freelance-payments
        
        # Check if we have local changes to commit
        if git diff --quiet && git diff --cached --quiet; then
          echo "ℹ️  No local changes to commit"
          exit 0
        fi
        
        # Stage our changes
        git add assets/jobs/*.json
        
        # Try to merge latest remote changes
        # Use merge (not rebase) to preserve our commit history
        if ! git merge origin/freelance-payments --no-edit; then
          echo "⚠️  Merge conflict detected - resolving by re-processing events"
          
          # Abort the merge
          git merge --abort
          
          # Reset to remote state to get clean file
          git reset --hard origin/freelance-payments
          
          # Re-run the Python script with latest file state and stored payload
          # This will re-read the file and apply our events on top of latest state
          echo "Re-processing events with latest file state..."
          if [ -f /tmp/event_payload.json ]; then
            export PAYLOAD_JSON=$(cat /tmp/event_payload.json)
          fi
          python .github/scripts/orchestration/user_exit_events.py
          
          # Stage changes again
          git add assets/jobs/*.json
        fi
        
        # Commit if there are changes
        if ! git diff --cached --quiet; then
          git commit -m "Update user behavior stats [skip ci]"
          
          # Push with simple retry
          MAX_RETRIES=3
          for i in $(seq 1 $MAX_RETRIES); do
            if git push origin freelance-payments; then
              echo "✅ Successfully pushed changes"
              exit 0
            else
              echo "⚠️  Push failed, attempt $i/$MAX_RETRIES"
              if [ $i -lt $MAX_RETRIES ]; then
                # Fetch latest and try again
                git fetch origin freelance-payments
                git rebase origin/freelance-payments || {
                  echo "❌ Rebase failed - aborting"
                  git rebase --abort || true
                  exit 1
                }
              fi
            fi
          done
          
          echo "❌ Failed to push after $MAX_RETRIES attempts"
          exit 1
        else
          echo "ℹ️  No changes to commit"
        fi
        shell: /usr/bin/bash -e {0}
        env:
          pythonLocation: /opt/hostedtoolcache/Python/3.11.14/x64
          PKG_CONFIG_PATH: /opt/hostedtoolcache/Python/3.11.14/x64/lib/pkgconfig
          Python_ROOT_DIR: /opt/hostedtoolcache/Python/3.11.14/x64
          Python2_ROOT_DIR: /opt/hostedtoolcache/Python/3.11.14/x64
          Python3_ROOT_DIR: /opt/hostedtoolcache/Python/3.11.14/x64
          LD_LIBRARY_PATH: /opt/hostedtoolcache/Python/3.11.14/x64/lib
      From https://github.com/seanivore/freelance-payments
      * branch            freelance-payments -> FETCH_HEAD
      Already up to date.
      [freelance-payments a339e04] Update user behavior stats [skip ci]
      1 file changed, 163 insertions(+), 163 deletions(-)
      To https://github.com/seanivore/freelance-payments
        1d14aad..a339e04  freelance-payments -> freelance-payments
      ✅ Successfully pushed changes
```

* **SECOND WORKFLOW: User Exit Events #35**

  + COMMIT: a339e04 — this commit actually shows all of the PREVIOUS workflows updates 
  + STATUS: Success — obviously not accurate, it saw two events but didn't act, and in Vercel you can see the two events were sent 

  + logging attempted and failed: 
    - `data.state.client_status.payment_1` — should get ISO timestamp — ❌ 
    - `price1.active` — should change from `true` to `false` — ❌ 

  1. PROCESSING EXIT EVENTS

```
      Run # Store payload for potential re-processing
        # Store payload for potential re-processing
        echo "$PAYLOAD_JSON" > /tmp/event_payload.json
        python .github/scripts/orchestration/user_exit_events.py || EXIT_CODE=$?
        if [ "${EXIT_CODE:-0}" -eq 2 ]; then
          echo "⚠️  Merge conflict detected - will resolve in commit step"
          exit 0  # Don't fail workflow, let commit step handle it
        elif [ "${EXIT_CODE:-0}" -ne 0 ]; then
          exit $EXIT_CODE  # Fail for other errors
        fi
        shell: /usr/bin/bash -e {0}
        env:
          pythonLocation: /opt/hostedtoolcache/Python/3.11.14/x64
          PKG_CONFIG_PATH: /opt/hostedtoolcache/Python/3.11.14/x64/lib/pkgconfig
          Python_ROOT_DIR: /opt/hostedtoolcache/Python/3.11.14/x64
          Python2_ROOT_DIR: /opt/hostedtoolcache/Python/3.11.14/x64
          Python3_ROOT_DIR: /opt/hostedtoolcache/Python/3.11.14/x64
          LD_LIBRARY_PATH: /opt/hostedtoolcache/Python/3.11.14/x64/lib
          STRIPE_SECRET_KEY: ***
          JOB_ID: uid-unc-480
          PAYLOAD_JSON: [{"type":"logged_in","timestamp":"2026-01-17T15:39:22.843Z","data":{}},{"type":"contract_loaded","timestamp":"2026-01-17T15:39:22.887Z","data":{"page":1,"totalPages":8}}]
      Processing 2 events for uid-unc-480...
      ℹ️  No state changes required.
```

  2. COMMIT CHANGES 

```
      Run git config --global user.name 'github-actions[bot]'
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        
        # CRITICAL FIX for BUG_01_010: Improved git operations to prevent conflicts
        # Strategy: Fetch latest, merge/rebase cleanly, handle conflicts by re-processing
        
        # Fetch latest changes
        git fetch origin freelance-payments
        
        # Check if we have local changes to commit
        if git diff --quiet && git diff --cached --quiet; then
          echo "ℹ️  No local changes to commit"
          exit 0
        fi
        
        # Stage our changes
        git add assets/jobs/*.json
        
        # Try to merge latest remote changes
        # Use merge (not rebase) to preserve our commit history
        if ! git merge origin/freelance-payments --no-edit; then
          echo "⚠️  Merge conflict detected - resolving by re-processing events"
          
          # Abort the merge
          git merge --abort
          
          # Reset to remote state to get clean file
          git reset --hard origin/freelance-payments
          
          # Re-run the Python script with latest file state and stored payload
          # This will re-read the file and apply our events on top of latest state
          echo "Re-processing events with latest file state..."
          if [ -f /tmp/event_payload.json ]; then
            export PAYLOAD_JSON=$(cat /tmp/event_payload.json)
          fi
          python .github/scripts/orchestration/user_exit_events.py
          
          # Stage changes again
          git add assets/jobs/*.json
        fi
        
        # Commit if there are changes
        if ! git diff --cached --quiet; then
          git commit -m "Update user behavior stats [skip ci]"
          
          # Push with simple retry
          MAX_RETRIES=3
          for i in $(seq 1 $MAX_RETRIES); do
            if git push origin freelance-payments; then
              echo "✅ Successfully pushed changes"
              exit 0
            else
              echo "⚠️  Push failed, attempt $i/$MAX_RETRIES"
              if [ $i -lt $MAX_RETRIES ]; then
                # Fetch latest and try again
                git fetch origin freelance-payments
                git rebase origin/freelance-payments || {
                  echo "❌ Rebase failed - aborting"
                  git rebase --abort || true
                  exit 1
                }
              fi
            fi
          done
          
          echo "❌ Failed to push after $MAX_RETRIES attempts"
          exit 1
        else
          echo "ℹ️  No changes to commit"
        fi
        shell: /usr/bin/bash -e {0}
        env:
          pythonLocation: /opt/hostedtoolcache/Python/3.11.14/x64
          PKG_CONFIG_PATH: /opt/hostedtoolcache/Python/3.11.14/x64/lib/pkgconfig
          Python_ROOT_DIR: /opt/hostedtoolcache/Python/3.11.14/x64
          Python2_ROOT_DIR: /opt/hostedtoolcache/Python/3.11.14/x64
          Python3_ROOT_DIR: /opt/hostedtoolcache/Python/3.11.14/x64
          LD_LIBRARY_PATH: /opt/hostedtoolcache/Python/3.11.14/x64/lib
      From https://github.com/seanivore/freelance-payments
      * branch            freelance-payments -> FETCH_HEAD
      ℹ️  No local changes to commit
```

### Preemptively Check Remaining Events 

  - `data.state.client_status.balance` 
  - `data.state.client_status.payment_2` 
  - `price2.active` 

### Return Users State Placement Error 

  * **Here are the console records** 
    + It jsut sends me straight to the start of the flow 
    + The contract is all that loads 
    + Looks like it can't detect progress 

```
          GET https://dev.payments.august.style/uid-unc-480 404 (Not Found)
        j @ assets/main-B0Q5IiWr.js:1
        await in j
        pv @ assets/index-CiALDdqw.js:8
        (anonymous) @ assets/index-CiALDdqw.js:8
        Bi @ assets/index-CiALDdqw.js:8
        Qc @ assets/index-CiALDdqw.js:8
        Pc @ assets/index-CiALDdqw.js:9
        Z1 @ assets/index-CiALDdqw.js:9
        job-CGBR-fQ2.js:50 Vite: job.tsx loaded
        job-CGBR-fQ2.js:50 No session_id found in URL or sessionStorage
        job-CGBR-fQ2.js:1 ✅ Loaded job data for uid-unc-480: {logged_in: null, contract_signed: null, invoice: null, payment_1: null, balance: null, …}
        job-CGBR-fQ2.js:50 🔍 State Management Debug: {sessionId: 'none', client_status: {…}}
        job-CGBR-fQ2.js:50 📍 Routing: no progress detected → contract (default)
        job-CGBR-fQ2.js:50 ✅ Final routing decision: contract
        job-CGBR-fQ2.js:50 Event: contract_loaded {page: 1, totalPages: 8}
        job-CGBR-fQ2.js:49 All 8 canvases ready, starting render...
        job-CGBR-fQ2.js:49 renderAllPages: Starting render for 8 pages
        job-CGBR-fQ2.js:49 Rendering page 1...
        job-CGBR-fQ2.js:49 Page 1 viewport: {width: 612, height: 1008, scale: 1}
        job-CGBR-fQ2.js:49 Canvas 1 dimensions: {internal: {…}, display: {…}, outputScale: 2}
        job-CGBR-fQ2.js:49 Starting render for page 1...
        job-CGBR-fQ2.js:49 Page 1 render completed
        job-CGBR-fQ2.js:49 Page 1 rendered successfully
        job-CGBR-fQ2.js:49 Rendering page 2...
        job-CGBR-fQ2.js:49 Page 2 viewport: {width: 612, height: 1008, scale: 1}
        job-CGBR-fQ2.js:49 Canvas 2 dimensions: {internal: {…}, display: {…}, outputScale: 2}
        job-CGBR-fQ2.js:49 Starting render for page 2...
        job-CGBR-fQ2.js:49 Page 2 render completed
        job-CGBR-fQ2.js:49 Page 2 rendered successfully
        job-CGBR-fQ2.js:49 Rendering page 3...
        job-CGBR-fQ2.js:49 Page 3 viewport: {width: 612, height: 1008, scale: 1}
        job-CGBR-fQ2.js:49 Canvas 3 dimensions: {internal: {…}, display: {…}, outputScale: 2}
        job-CGBR-fQ2.js:49 Starting render for page 3...
        job-CGBR-fQ2.js:49 Page 3 render completed
        job-CGBR-fQ2.js:49 Page 3 rendered successfully
        job-CGBR-fQ2.js:49 Rendering page 4...
        job-CGBR-fQ2.js:49 Page 4 viewport: {width: 612, height: 1008, scale: 1}
        job-CGBR-fQ2.js:49 Canvas 4 dimensions: {internal: {…}, display: {…}, outputScale: 2}
        job-CGBR-fQ2.js:49 Starting render for page 4...
        job-CGBR-fQ2.js:49 Page 4 render completed
        job-CGBR-fQ2.js:49 Page 4 rendered successfully
        job-CGBR-fQ2.js:49 Rendering page 5...
        job-CGBR-fQ2.js:49 Page 5 viewport: {width: 612, height: 1008, scale: 1}
        job-CGBR-fQ2.js:49 Canvas 5 dimensions: {internal: {…}, display: {…}, outputScale: 2}
        job-CGBR-fQ2.js:49 Starting render for page 5...
        job-CGBR-fQ2.js:49 Page 5 render completed
        job-CGBR-fQ2.js:49 Page 5 rendered successfully
        job-CGBR-fQ2.js:49 Rendering page 6...
        job-CGBR-fQ2.js:49 Page 6 viewport: {width: 612, height: 1008, scale: 1}
        job-CGBR-fQ2.js:49 Canvas 6 dimensions: {internal: {…}, display: {…}, outputScale: 2}
        job-CGBR-fQ2.js:49 Starting render for page 6...
        job-CGBR-fQ2.js:49 Page 6 render completed
        job-CGBR-fQ2.js:49 Page 6 rendered successfully
        job-CGBR-fQ2.js:49 Rendering page 7...
        job-CGBR-fQ2.js:49 Page 7 viewport: {width: 612, height: 1008, scale: 1}
        job-CGBR-fQ2.js:49 Canvas 7 dimensions: {internal: {…}, display: {…}, outputScale: 2}
        job-CGBR-fQ2.js:49 Starting render for page 7...
        job-CGBR-fQ2.js:49 Page 7 render completed
        job-CGBR-fQ2.js:49 Page 7 rendered successfully
        job-CGBR-fQ2.js:49 Rendering page 8...
        job-CGBR-fQ2.js:49 Page 8 viewport: {width: 612, height: 1008, scale: 1}
        job-CGBR-fQ2.js:49 Canvas 8 dimensions: {internal: {…}, display: {…}, outputScale: 2}
        job-CGBR-fQ2.js:49 Starting render for page 8...
        job-CGBR-fQ2.js:49 Page 8 render completed
        job-CGBR-fQ2.js:49 Page 8 rendered successfully
```

---

### BUG_01_017 - Return URL Should Check Session Status (Stripe Best Practice)

**Date**: 2026-01-17  
**Status**: Fixed

**Issue**: Return URL handling was using `complete=` URL parameter instead of checking Checkout Session status directly. According to Stripe best practices, we should:
1. Check session status immediately when return page loads
2. Handle `complete` status (success) → show completion page
3. Handle `open` status (failed/canceled) → remount checkout

**Root Cause**: 
- App was routing based on `complete=` URL parameter instead of session status
- No handling for `open` status (failed/canceled payments)
- `Complete.tsx` component existed but wasn't being used

**Fixes Implemented** (2026-01-17):
- **Updated session-status API** (`api/session-status.js`):
  - Now returns `metadata` field (includes `payment_number`)
- **Updated App.tsx**:
  - Added `sessionStatus` and `sessionPaymentNumber` state
  - Check session status immediately when `session_id` detected
  - Route based on session status:
    - `complete` → show CompletionView (determine payment_1 vs payment_2 from metadata)
    - `open` → remount checkout (show PaymentView again)
  - Removed dependency on `complete=` URL parameter
  - Updated return URL to only include `session_id` (not `complete=` parameter)
- **Removed unused component**:
  - Deleted `src/components/Complete.tsx` (not being used, functionality moved to App.tsx)

**Files Modified**:
- `api/session-status.js`: Added metadata to response
- `src/App.tsx`: Added session status checking and routing logic
- `src/components/Complete.tsx`: Deleted (unused)

**Expected Result**: 
- Return URL checks session status immediately (Stripe best practice)
- Handles both success (`complete`) and failure (`open`) cases
- Payment number determined from session metadata (not URL parameter)
- Cleaner URL (no `complete=` parameter needed)

* **THIRD WORKFLOW: User Exit Events #36**

  + COMMIT: 8a215e0 — workflow checked out this commit (the one that created Stripe objects and PDFs)
  + STATUS: Failure — "error: src refspec freelance-payments does not match any"
  
  + Root Cause Identified:
    - Workflow was checking out a specific commit SHA (`ref: ${{ github.sha }}`) instead of the branch
    - This put the workflow in detached HEAD state
    - When trying to push, git couldn't find a local `freelance-payments` branch to push
    - The commit reference was correct (8a215e0), but the checkout method was wrong

  + Error Log:
```
    [detached HEAD 487cfb7] Update user behavior stats [skip ci]
     1 file changed, 163 insertions(+), 163 deletions(-)
    error: src refspec freelance-payments does not match any
    error: failed to push some refs to 'https://github.com/seanivore/freelance-payments'
    ⚠️  Push failed, attempt 1/3
    ...
    ❌ Failed to push after 3 attempts
```

* **FOURTH WORKFLOW: User Exit Events #37**

  + COMMIT: 8a215e0 — same issue as #36
  + STATUS: Failure — same error as #36
  
  + Same root cause: detached HEAD state preventing push

### Fix Implemented (2026-01-17)

**Root Cause**: Workflow was checking out a specific commit SHA (`ref: ${{ github.sha }}`) which put it in detached HEAD state. When trying to push to `freelance-payments` branch, git couldn't find a local branch reference.

**Solution**: 
- Changed checkout to use branch name (`ref: freelance-payments`) instead of commit SHA
- Added safety check in commit step to ensure we're on the branch (not detached HEAD)
- If in detached HEAD state, checkout the branch before proceeding

**Files Modified**:
- `.github/workflows/user-exit-events.yml`: 
  - Changed `ref: ${{ github.sha }}` to `ref: freelance-payments`
  - Added branch check in commit step to handle edge cases

**Expected Result**: 
- Workflow always checks out the branch (not detached HEAD)
- Push operations succeed because local branch exists
- Events are processed and committed correctly